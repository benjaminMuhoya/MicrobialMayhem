import test from "node:test";import assert from "node:assert/strict";import ts from "typescript";import fs from "node:fs";import vm from "node:vm";const source=fs.readFileSync(new URL("../app/game/performance.ts",import.meta.url),"utf8"),js=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText,loaded={exports:{}};vm.runInNewContext(`(function(exports,module){${js}})(loaded.exports,loaded)`,{loaded});const {chooseQualityTier,QUALITY_LIMITS}=loaded.exports;test("older devices receive bounded visual effects",()=>{assert.equal(chooseQualityTier({memory:2,cores:2,width:1024,reducedMotion:false}),"low");assert.equal(chooseQualityTier({memory:4,cores:4,width:900,reducedMotion:false}),"medium");assert.equal(chooseQualityTier({memory:8,cores:8,width:1200,reducedMotion:false}),"high");assert.equal(QUALITY_LIMITS.low.particles,8)});test("reduced motion always selects the calm tier",()=>assert.equal(chooseQualityTier({memory:8,cores:8,width:1200,reducedMotion:true}),"low"));
