import test from "node:test";import assert from "node:assert/strict";import ts from "typescript";import fs from "node:fs";import vm from "node:vm";const source=fs.readFileSync(new URL("../app/game/accessibility.ts",import.meta.url),"utf8"),js=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText,loaded={exports:{}};vm.runInNewContext(`(function(exports,module){${js}})(loaded.exports,loaded)`,{loaded});const {nextFocusIndex,isActivationButton}=loaded.exports;test("keyboard and controller focus wraps predictably",()=>{assert.equal(nextFocusIndex(2,3,1),0);assert.equal(nextFocusIndex(0,3,-1),2);assert.equal(nextFocusIndex(-1,3,1),0)});test("standard controller face buttons activate",()=>{assert.equal(isActivationButton(0),true);assert.equal(isActivationButton(2),false)});
